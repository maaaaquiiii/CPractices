#ifndef ROTAR_H
    #define ROTAR_H

    #define N 10
    #define M 15
    
    void imprimir(char[M][N]);
    void rellenar(char[M][N]);
    void rotar(char[M][N]);
    void desrotar(char[M][N]);
    void matToFile(char[M][N], FILE*);
    int  randomNum(int, int);

#endif 
